# Build and deploy to Cloud Run from the svg-to-stl directory.
# Run: gcloud builds submit --config=cloudbuild.yaml .
steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/${PROJECT_ID}/svg-to-stl:${BUILD_ID}"
      - "-t"
      - "gcr.io/${PROJECT_ID}/svg-to-stl:latest"
      - "."
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/${PROJECT_ID}/svg-to-stl:${BUILD_ID}"
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/${PROJECT_ID}/svg-to-stl:latest"
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - "run"
      - "deploy"
      - "svg-to-stl"
      - "--image"
      - "gcr.io/${PROJECT_ID}/svg-to-stl:${BUILD_ID}"
      - "--region"
      - "${_REGION}"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
substitutions:
  _REGION: us-central1
options:
  logging: CLOUD_LOGGING_ONLY
